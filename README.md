# Nex Studio

A premium creator marketing platform website template, inspired by Billo.app but featuring a **Gold & Dark** aesthetic.

## 🚀 How to Run

Simply open the `index.html` file in any web browser.

No installation (Node.js/npm) is required for this version.

## 🎨 Design Features

- **Theme**: Dark Mode (`#0a0a0a`) with Gold Accents (`#f59e0b`).
- **Typography**: Inter (via Google Fonts).
- **Responsive**: Fully responsive grid layouts.
- **Effects**: Glassmorphism navbar, hover transitions, and scroll-reveal animations.

## 📁 File Structure

- `index.html` - Main structure and content.
- `style.css` - All styles (Tokens, Utilities, Components).
- `main.js` - Simple interactivity (Scroll animations).
